// Klo mau pake, pake aja ini bkn enc cma terser aja

import https from"https";import fetch from"node-fetch";let handler=async(m,{args:args,usedPrefix:usedPrefix,command:command})=>{if(!args[0])throw`Ex: ${usedPrefix+command} nekopoi.care`;let res=await checkWeb(args);m.reply(res.map((v=>`*• Domain:* ${v.Domain}\n*• Status:* ${v.Status}`)).join("\n\n"))};handler.command=/^web(check|cek)|(check|cek)web$/i;export default handler;async function checkWeb(url){return(await(await fetch("https://trustpositif.kominfo.go.id/Rest_server/getrecordsname_home",{agent:new https.Agent({rejectUnauthorized:!1}),method:"post",body:new URLSearchParams(Object.entries({name:url.join("%0A")}))})).json()).values}