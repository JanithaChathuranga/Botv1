// Klo mau pake, pake aja ini bkn enc cma terser aja

import fetch from"node-fetch";let handler=async(m,{conn:conn,text:text,usedPrefix:usedPrefix,command:command})=>{if(!text)throw`Input Query\nExample : ${usedPrefix+command} 1girl, blush, looking to viewer, warm smile`;let randomS=randomId(),res=await fetch(global.API("can","/api/maker/diffusion",{text:encodeURIComponent(m.text),seed:randomS},"apikey")),json=await res.json();if(!json.base64Img)throw"Server Error 404";let buffer=Buffer.from(json.base64Img,"base64");try{await conn.sendFile(m.chat,buffer,"","Request from : "+text,m)}catch(e){console.log(e),m.reply("Terjadi kesalahan atau server sedang mengalami gangguan.")}};handler.help=["diffusion"].map((v=>v+" <query>")),handler.tags=["tools"],handler.command=/^(diffusion|ai-image)$/i;export default handler;function randomId(){return Math.floor(1e8+1e8*Math.random())}