// Klo mau pake, pake aja ini bkn enc cma terser aja

import fetch from"node-fetch";import cheerio from"cheerio";let handler=async(m,{conn:conn,args:args,text:text})=>{if(await m.reply(wait),!text)return m.reply("input query apkmirror?");try{let list=(await SearchApk(text)).map(((item,index)=>`\n\n*Title:* ${item.titles}\n*Url:* ${item.value}`)).join("\n"),tops=`*${htki} 📺 Apk Search 🔎 ${htka}*`;await m.reply(tops+list)}catch(e){await m.reply(eror)}};handler.help=["apkms <query>"],handler.tags=["internet"],handler.command=/^(apkms)$/i;export default handler;async function SearchApk(query){const result=[];return await fetch("https://www.apkmirror.com/?s="+query).then((response=>response.text())).then((data=>{const $=cheerio.load(data);return $(".appRow").each(((index,element)=>{const link=$(element).find("a").attr("href"),titles=$(element).find("a").text();link.startsWith("/apk")&&result.push({titles:titles.split("\n")[0],value:"https://www.apkmirror.com"+link.split("#")[0]})})),result}))}